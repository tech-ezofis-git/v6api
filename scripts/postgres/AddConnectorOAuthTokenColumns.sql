-- Replaced by Create-Connector-Table.sql (modern schema) -- Postgres.
-- Ported from scripts/AddConnectorOAuthTokenColumns.sql -- Phase 3.
-- Kept for compatibility: forwards to the modern create/migrate script, same as the
-- SQL Server original (which was already marked obsolete there, not just here).
-- Run: psql ... -f scripts/postgres/Create-Connector-Table.sql

-- 'AddConnectorOAuthTokenColumns.sql is obsolete. Use Create-Connector-Table.sql instead.'
